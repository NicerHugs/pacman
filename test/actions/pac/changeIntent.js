import {expect} from 'chai';

describe('pac reducer after CHANGE_INTENT action', () => {
	it('should change the intent for up');
	it('should change the intent for down');
	it('should change the intent for left');
	it('should change the intent for right');
});
