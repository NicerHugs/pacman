import {expect} from 'chai';

describe('pac reducer after MOVE_BODIES action', () => {
	it('should change pac\'s position based on its direction if not centered in a grid square');
	it('should update pac\'s direction based on intent if centered in a grid square');
	it('should change pac\'s position based on intent if centered in a grid square');
	it('should not change pac\'s position if pac would go into a wall');
});
